<div class="posts_list">
    <%= button_to(image_tag('mingcute--arrow-left-fill.svg', style: 'width:30px;').html_safe, 
        root_path, 
        method: :get,
        class: 'button_fig') %>
    <%= button_to "Novo Post", post_path, method: :get, class: 'btn btn-primary' %>
    <% @user_posts.each do |post| %>
        <div class="card">
            <div class="post_title">
                <%= post.title %>
            </div>
            <div class="buttons_post"> 
                <%= button_to(image_tag('mdi--pencil.svg', style: 'width:30px;').html_safe, 
                    post_path + '/' + post.id.to_s,
                    method: :get,
                    class: 'button_fig',
                    style: 'width: 10%;') %>
                <%= button_to(image_tag('mdi--trash.svg', style: 'width:30px;').html_safe, 
                    post_path + '/' + post.id.to_s, 
                    method: :get,
                    class: 'button_fig',
                    style: 'width: 10%;') %>
            </div>
        </div>
    <% end %>
</div>